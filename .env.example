# ---------------------------------------------------------------
# Perfex CRM — Dokploy Environment Variables
# Copy these into Dokploy → your Compose service → Environment tab
# Never commit a real .env file with actual secrets to git
# ---------------------------------------------------------------

# ---------------------------------------------------------------
# Application
# ---------------------------------------------------------------

# Full base URL of your site — MUST end with a trailing slash
APP_BASE_URL=https://crm.yourdomain.com/

# Domain only (no https://) — used by Traefik for routing
APP_DOMAIN=crm.yourdomain.com

# Encryption key — generate once, NEVER change after first install
# Generate with:  openssl rand -hex 16
APP_ENC_KEY=replace_with_32_char_random_string

# ---------------------------------------------------------------
# MySQL Database
# ---------------------------------------------------------------

# Root password for the MySQL container
MYSQL_ROOT_PASSWORD=replace_with_strong_root_password

# Database name, user, and password that Perfex CRM will use
MYSQL_DATABASE=perfex_crm
MYSQL_USER=perfex
MYSQL_PASSWORD=replace_with_strong_db_password
